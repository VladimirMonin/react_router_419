# 🛒 Гибридная корзина и оформление заказов в React

> **Конспект живой кодинг-сессии**  
> Детальное объяснение каждого шага с рассуждениями о том, ПОЧЕМУ мы делаем именно так,  
> а не просто "что нужно сделать".

## 📋 Содержание

1. [Проблемы текущей реализации](#проблемы-текущей-реализации)
2. [Решение: Гибридная архитектура](#решение-гибридная-архитектура)
3. [Коммит №1: Расширение API слоя](#коммит-1-расширение-api-слоя)
4. [Коммит №2: Гибридная логика корзины](#коммит-2-гибридная-логика-корзины)
5. [Коммит №3: Синхронизация при входе](#коммит-3-синхронизация-при-входе)
6. [Коммит №4: Оформление заказа](#коммит-4-оформление-заказа)
7. [Коммит №5: История заказов](#коммит-5-история-заказов)
8. [Выводы и принципы проектирования](#выводы-и-принципы-проектирования)

---

## Проблемы текущей реализации

### Что у нас есть сейчас?

На данный момент наше приложение — это **витрина с локальной корзиной в памяти**. Корзина живёт в `useState` внутри React-компонента. Давай честно разберём, какие проблемы это создаёт в реальной жизни.

### Проблема №1: Потеря данных при закрытии вкладки

**Сценарий из жизни:**
Пользователь (назовём его Вася) заходит в наш магазин. Он потратил 15 минут, выбирая товары, добавил 5 позиций в корзину. Потом решил поискать отзывы о товаре в Google, открыл новую вкладку... и случайно закрыл нашу. Открывает снова — **корзина пустая**.

**Почему так происходит?**
Корзина живёт в `useState`. Это React state, который хранится в оперативной памяти (ОЗУ). Когда страница выгружается из браузера, вся память очищается. Это как блокнот, который существует только пока ты его держишь в руках — положил на стол и ушёл, он исчез.

**Что чувствует Вася?**
Разочарование. Он потратил время, а результат пропал. Скорее всего, он просто уйдёт к конкурентам, у которых корзина не обнуляется.

**Почему мы не можем просто игнорировать эту проблему?**
Потому что в реальном e-commerce пользователи редко покупают моментально. Они сравнивают цены на других сайтах, читают отзывы, консультируются с друзьями. Процесс покупки может занимать часы или дни. Если корзина не сохраняется — мы теряем продажи.

### Проблема №2: Разные корзины на разных устройствах

**Сценарий:**
Вася (наш упорный покупатель) зашёл с рабочего компьютера, добавил товары. Вечером, в метро, хочет завершить покупку с телефона. Открывает наш сайт — **корзина пустая**. Хотя на компьютере она была заполнена!

**Почему так?**
Потому что даже если бы мы сохраняли корзину в `localStorage` (локальное хранилище браузера), это хранилище **привязано к конкретному браузеру на конкретном устройстве**. Это как записная книжка, которая лежит дома — когда ты на работе, ты к ней не имеешь доступа.

**Что это значит для бизнеса?**
В 2025 году люди используют минимум 2-3 устройства: компьютер, смартфон, планшет. По статистике, около 40% покупок начинаются на одном устройстве, а заканчиваются на другом. Если мы не поддерживаем такой сценарий — мы теряем почти половину потенциальных покупателей.

### Проблема №3: Невозможно оформить заказ

**Текущая ситуация:**
У нас есть корзина, но нет механизма превращения корзины в заказ. Пользователь видит товары, но кнопка "Оформить заказ" просто не работает. Это как магазин с витринами, но без кассы.

**Что нужно для заказа?**

1. Адрес доставки
2. Контактный телефон  
3. Фиксация цен (чтобы завтра, когда цена изменится, заказ остался по старой цене)
4. Сохранение в базу данных
5. История заказов для пользователя

Ничего из этого у нас сейчас нет.

**Почему это проблема?**
Потому что корзина без возможности оформить заказ — это бессмысленно. Это всё равно что автомобиль без руля. Можно смотреть, но нельзя использовать.

---

## Решение: Гибридная архитектура

### Что такое "гибридная" архитектура?

Гибридная — значит работающая по-разному в зависимости от ситуации. У нас будет **две стратегии хранения**, и приложение само будет выбирать нужную:

#### Стратегия №1: Для гостей (неавторизованных пользователей)

**Где хранится корзина:** В `localStorage` браузера.

**Почему именно localStorage?**

- ✅ Данные не пропадают при закрытии вкладки
- ✅ Не требует авторизации (гость может накидать товары до регистрации)
- ✅ Работает мгновенно (нет сетевых запросов)
- ❌ Но привязано к устройству (на телефоне не увидит)
- ❌ Но нельзя оформить заказ (нет связи с сервером)

**Как это работает:**

```
Гость добавляет товар
    ↓
Записываем в localStorage
    ↓
При перезагрузке страницы — читаем обратно
```

#### Стратегия №2: Для авторизованных пользователей

**Где хранится корзина:** На сервере (PostgreSQL).

**Почему именно сервер?**

- ✅ Доступна с любого устройства (телефон, компьютер, планшет)
- ✅ Можно оформить заказ
- ✅ Можно вести историю покупок
- ✅ Надёжное хранение (не пропадёт даже если очистить кеш браузера)
- ❌ Но требует интернета
- ❌ Но требует авторизации

**Как это работает:**

```
Пользователь добавляет товар
    ↓
Отправляем запрос на сервер: POST /cart/items
    ↓
Сервер сохраняет в БД, привязывая к user_id
    ↓
При входе с другого устройства — сервер вернёт те же данные
```

### Самое сложное: переход от гостя к пользователю

**Проблема:**
Вася пришёл гостем, накидал 5 товаров в корзину (они в localStorage). Потом решил зарегистрироваться. Что должно произойти с его 5 товарами?

**Плохое решение:** Просто загрузить корзину с сервера (она пустая) и забыть про localStorage.  
**Результат:** Вася потерял свои 5 товаров. Он в ярости уходит.

**Хорошее решение:** Объединить (merge) localStorage и серверную корзину.  
**Результат:** На сервере теперь 5 товаров Васи, он доволен, готов покупать.

**Но есть нюанс:** А что если у Васи уже была корзина на сервере (он раньше заходил с другого устройства), и там был 1 товар? И сейчас он добавил ещё 3 как гость?

**Правильный ответ:** Сервер должен **суммировать количество**:

- Товар A был на сервере: 1 шт
- Товар A в localStorage: 2 шт
- Итого на сервере станет: 3 шт (1 + 2)

Это называется **Upsert** (Update или Insert):

- Если товар уже есть → увеличиваем количество
- Если товара нет → добавляем новый

### Визуализация: До и После

**ДО рефакторинга:**

```
👤 Гость → 🛒 Память (useState) → ❌ Закрыл вкладку = потерял
👤 Юзер → 🛒 Память (useState) → ❌ Закрыл вкладку = потерял  
❌ Нет заказов
❌ Нет истории
```

**ПОСЛЕ рефакторинга:**

```
👤 Гость → 💾 localStorage → ✅ Сохранилось между сессиями
                            → При логине → merge на сервер

👤 Юзер → ☁️ PostgreSQL → ✅ Доступно с любого устройства
                        → ✅ Можно оформить заказ  
                        → ✅ Есть история покупок
```

### Ключевые принципы решения

1. **Прозрачность для UI**  
   Компоненты (ProductCard, CartPage) **не знают**, где хранится корзина. Они просто вызывают `addToCart()`. Хук `useCart` сам решает: писать в localStorage или слать на сервер.

2. **Graceful Degradation (постепенная деградация)**  
   Если сервер недоступен (нет интернета) — гость всё равно может добавлять товары в localStorage. Сайт не "ломается", просто работает в ограниченном режиме.

3. **Единый источник правды**  
   Для авторизованных — это сервер. Локальное состояние (`items` в useState) — это просто кеш для быстрого отображения. Настоящая правда всегда на сервере.

4. **Оптимистичный UI**  
   Когда юзер жмёт "добавить в корзину", мы **сразу** обновляем интерфейс (он видит +1 товар). Потом отправляем запрос на сервер в фоне. Если запрос упал — откатываем изменения. Но чаще всего запрос успешен, и пользователь видит мгновенную реакцию.

---

## Коммит 1: Расширение API слоя

### Зачем нужен этот коммит?

Перед тем как менять логику компонентов, нам нужно **научить фронтенд разговаривать с новыми эндпоинтами бэкенда**. Это как изучить иностранный язык перед поездкой в другую страну — сначала учим слова, потом едем.

Бэкенд уже готов (у нас есть `openapi.json` с описанием API), но фронтенд про эти эндпоинты ничего не знает. Нет типов данных, нет функций для запросов. Этот коммит — "мост" между фронтендом и бэкендом.

### Что мы добавляем?

#### 1. Новые типы данных (`src/types/api.ts`)

**ServerCartItem** — позиция в корзине на сервере:

```typescript
export interface ServerCartItem {
  id: number;          // ID позиции в БД
  product_id: number;  // Ссылка на товар
  quantity: number;    // Количество
  product: Product;    // Вложенный объект товара
}
```

**Почему `product` вложенный объект?**  
Представь, что сервер вернул бы только `product_id: 42`. Что дальше? Нам пришлось бы делать **второй запрос**: "А дай-ка мне Product с id=42". Это медленно (два запроса вместо одного) и сложно (нужно управлять двумя асинхронными операциями).

Поэтому сервер сразу отдаёт полные данные товара. Это называется **Eager Loading** (жадная загрузка) — "загрузил всё заранее". Да, ответ получается больше по размеру, но зато один запрос вместо N+1.

**Cart** — корзина целиком:

```typescript
export interface Cart {
  id: number;                  // ID корзины
  items: ServerCartItem[];     // Массив позиций
  total_price: number;         // Сумма (считает сервер!)
}
```

**Почему сервер считает `total_price`?**  
Можно было бы считать на фронтенде: `items.reduce((sum, item) => sum + item.product.price * item.quantity, 0)`. Но это **опасно** по двум причинам:

1. **Рассинхрон данных:** Если на фронте какая-то ошибка округления (0.1 + 0.2 = 0.30000000000000004 в JS), фронт покажет одну сумму, а бэк

енд при оформлении заказа посчитает другую. Пользователь в шоке.

2. **Безопасность:** Злоумышленник может поменять JS-код в браузере и показать себе сумму 0. Да, при оформлении заказа сервер всё равно пересчитает, но это создаёт путаницу.

Поэтому **единый источник правды для денег** — это сервер. Он считает, он прав.

**Order** — заказ:

```typescript
export interface Order {
  id: number;
  status: 'pending' | 'confirmed' | 'shipped' | 'delivered' | 'cancelled';
  total_amount: number;        // Зафиксированная сумма
  created_at: string;          // ISO дата (2025-11-26T12:00:00)
  delivery_address: string;
  phone?: string;
  items: OrderItem[];          // С frozen_price!
}
```

**OrderItem** — позиция заказа:

```typescript
export interface OrderItem {
  id: number;
  product_id: number;
  quantity: number;
  frozen_price: number;        // ⭐ Ключевое поле!
  product_name: string;        // Название на момент заказа
}
```

**Почему `frozen_price` (замороженная цена)?**

Это **очень важная концепция** в e-commerce. Давай разберём на примере:

```
День 1 (26 ноября):
- Портальная пушка стоит 500 шмеклей
- Вася заказал 2 штуки
- В заказе записалось: frozen_price = 500

День 2 (27 ноября):
- Менеджер поднял цену до 600 шмеклей (товар популярный!)
- В таблице products.price теперь 600

День 3 (28 ноября):
- Вася открывает историю заказов
- Видит: "Портальная пушка, 2 шт × 500 = 1000"
```

Если бы мы не сохраняли `frozen_price`, а просто хранили `product_id`, то при просмотре истории пришлось бы брать **текущую** цену из таблицы `products`. И Вася увидел бы 2 × 600 = 1200. Он в шоке: "Я платил 1000, а тут 1200!".

Поэтому при создании заказа мы **фиксируем цену навсегда**. Даже если завтра товар будет стоить 1 миллион — в заказе №42 навечно останется 500.

#### 2. Новые методы API (`src/services/api.ts`)

**cartApi** — работа с корзиной:

```typescript
export const cartApi = {
  get(): Promise<Cart>,              // Получить корзину
  addItem(data): Promise<ServerCartItem>,     // Добавить товар
  updateQuantity(id, data): Promise<ServerCartItem>,  // Изменить количество
  removeItem(id): Promise<void>,     // Удалить товар
  merge(data): Promise<Cart>         // Слить корзины (самое важное!)
}
```

**Метод `merge` — почему он ключевой?**

Это наш "мост" между миром гостя и миром авторизованного пользователя. Когда гость логинится, мы вызываем:

```typescript
await cartApi.merge({
  items: [
    { product_id: 1, quantity: 2 },
    { product_id: 3, quantity: 1 }
  ]
})
```

Сервер получает этот массив и **интеллектуально объединяет** с тем, что уже есть в базе:

```
Было на сервере:  Товар 1: 1 шт, Товар 5: 3 шт
Пришло от клиента: Товар 1: 2 шт, Товар 3: 1 шт

Стало на сервере:  Товар 1: 3 шт (1+2), Товар 3: 1 шт, Товар 5: 3 шт
```

Это **Upsert** (UPDATE + INSERT):

- Товар 1 уже был → обновили количество (1+2)
- Товар 3 не было → вставили новый
- Товар 5 не трогали → остался как был

**ordersApi** — работа с заказами:

```typescript
export const ordersApi = {
  create(data): Promise<Order>,      // Создать заказ
  getAll(): Promise<Order[]>,        // Получить все заказы юзера
  getById(id): Promise<Order>        // Получить один заказ
}
```

**Почему в `create` не передаём список товаров?**

Обрати внимание: `OrderCreate` содержит только `delivery_address` и `phone`. Без списка товаров! Почему?

Потому что сервер **сам возьмёт товары из корзины** текущего пользователя. Это удобно по двум причинам:

1. **Меньше данных по сети** — не нужно слать огромный массив товаров
2. **Безопасность** — пользователь не может "подложить" свой список с изменёнными ценами

Алгоритм на бэкенде:

```python
def create_order(user_id, address):
    # 1. Берём корзину пользователя
    cart = db.query(Cart).filter_by(user_id=user_id).first()
    
    # 2. Создаём заказ
    order = Order(user_id=user_id, address=address)
    
    # 3. Копируем товары с frozen_price
    for cart_item in cart.items:
        order.items.append(OrderItem(
            product_id=cart_item.product_id,
            quantity=cart_item.quantity,
            frozen_price=cart_item.product.price  # ⭐ Фиксируем!
        ))
    
    # 4. Очищаем корзину (она больше не нужна)
    cart.items.clear()
    
    # 5. Сохраняем
    db.add(order)
    db.commit()
```

#### 3. Авторизованные запросы

Все методы `cartApi` и `ordersApi` требуют токен. Мы сделали вспомогательную функцию:

```typescript
const authorizedFetch = async (url: string, options: RequestInit = {}): Promise<Response> => {
  const token = localStorage.getItem('token');
  
  if (!token) {
    throw new Error('Необходима авторизация');
  }

  const headers = new Headers(options.headers);
  headers.set('Authorization', `Bearer ${token}`);
  
  const response = await fetch(url, { ...options, headers });
  
  if (response.status === 401) {
    throw new Error('Сессия истекла');
  }
  
  return response;
}
```

**Почему отдельная функция?**

Без неё каждый метод выглядел бы так:

```typescript
async addItem(data) {
  const token = localStorage.getItem('token');  // Повторяется!
  const headers = { 'Authorization': `Bearer ${token}` };  // Повторяется!
  const response = await fetch(url, { headers, body: ... });
  if (response.status === 401) { ... }  // Повторяется!
  return response.json();
}
```

Это **дублирование кода**. А дублирование — это зло, потому что:

- Если нужно изменить (например, добавить логирование) — придётся менять в 10 местах
- Легко допустить ошибку в одном месте

С `authorizedFetch` весь общий код в одном месте. Это принцип **DRY** (Don't Repeat Yourself).

### Итог коммита №1

Теперь у нас есть:
✅ Типы данных, соответствующие контракту API  
✅ Функции для всех нужных запросов  
✅ Обработка авторизации в одном месте  

Фронтенд **готов разговаривать с бэкендом**, но UI ещё не использует новый API. Это сделаем в следующем коммите.

---

## Коммит 2: Гибридная логика корзины

### Зачем нужен этот коммит?

Сейчас хук `useCart` просто хранит массив в `useState` и всё. Ничего не сохраняется. Мы хотим сделать его **умным** — чтобы он сам решал, куда писать данные.

### Проблема: Откуда хук знает, гость это или юзер?

Хуку нужно знать, авторизован ли пользователь. Информация об этом есть в `AuthContext` (`user`). Значит, `useCart` должен использовать `useAuth`.

**Но тут есть важный нюанс с порядком провайдеров!**

Если сделать так (НЕПРАВИЛЬНО):

```tsx
<CartProvider>
  <AuthProvider>
    <App />
  </AuthProvider>
</CartProvider>
```

То `CartProvider` **не имеет доступа** к `AuthContext`, потому что находится **выше** по дереву. Это как спрашивать у родителя про его ещё не родившегося ребёнка.

Правильно (ИСПРАВЛЕНО):

```tsx
<AuthProvider>
  <CartProvider>
    <App />
  </CartProvider>
</AuthProvider>
```

Теперь `CartProvider` — "ребёнок" `AuthProvider`, и может использовать `useAuth()`.

### Структура данных: CartItem

Нам нужно **два представления** одних и тех же данных:

**Серверное** (`ServerCartItem` из API):

```typescript
{
  id: 42,              // ID позиции на сервере
  product_id: 1,
  quantity: 2,
  product: { id: 1, name: "...", price: 500, ... }
}
```

**Локальное** (для UI и localStorage):

```typescript
{
  product: { id: 1, name: "...", price: 500, ... },
  quantity: 2,
  serverItemId?: 42   // Опциональное! Есть только у авторизованных
}
```

**Почему два формата?**

Потому что `localStorage` не умеет хранить связи между таблицами (это не БД). Проще всего хранить целиком объект `product`. А `serverItemId` нужен для обновлений — чтобы знать, какую позицию менять на сервере.

### Логика работы

#### При загрузке (useEffect)

```typescript
useEffect(() => {
  fetchCart();
}, [user]);  // Реагируем на изменение пользователя
```

**Почему зависимость `[user]`?**

Потому что когда пользователь входит (`user` меняется с `null` на объект) или выходит (наоборот), нам нужно **перезагрузить корзину** из нового источника:

```
user = null → user = {...}   (Вход)
    ↓
fetchCart() загружает с сервера

user = {...} → user = null   (Выход)
    ↓
fetchCart() загружает из localStorage
```

#### fetchCart — загрузка корзины

```typescript
const fetchCart = useCallback(async () => {
  if (user) {
    // Авторизован → с сервера
    const serverCart = await cartApi.get();
    const cartItems = serverCart.items.map(item => ({
      product: item.product,
      quantity: item.quantity,
      serverItemId: item.id  // Сохраняем для обновлений
    }));
    setItems(cartItems);
  } else {
    // Гость → из localStorage
    const localItems = loadFromLocalStorage();
    setItems(localItems);
  }
}, [user]);
```

**Почему `useCallback`?**

Потому что `fetchCart` используется в `useEffect` с зависимостью `[fetchCart]`. Если бы мы не обернули в `useCallback`, функция пересоздавалась бы **при каждом рендере**, и useEffect запускался бы бесконечно (бесконечный цикл).

`useCallback` гарантирует: "Пересоздавай функцию только когда изменится `user`".

#### addToCart — добавление товара

```typescript
const addToCart = async (product: Product) => {
  if (user) {
    // 1. Отправляем на сервер
    const newItem = await cartApi.addItem({
      product_id: product.id,
      quantity: 1
    });
    
    // 2. Обновляем локальный state
    setItems(prevItems => {
      const existingIndex = prevItems.findIndex(item => item.product.id === product.id);
      
      if (existingIndex >= 0) {
        // Товар уже был → обновляем
        return prevItems.map((item, index) =>
          index === existingIndex
            ? { ...item, quantity: newItem.quantity, serverItemId: newItem.id }
            : item
        );
      } else {
        // Новый товар
        return [...prevItems, {
          product: newItem.product,
          quantity: newItem.quantity,
          serverItemId: newItem.id
        }];
      }
    });
  } else {
    // Гость → в localStorage
    setItems(prevItems => {
      const existingItem = prevItems.find(item => item.product.id === product.id);
      
      let newItems;
      if (existingItem) {
        newItems = prevItems.map(item =>
          item.product.id === product.id
            ? { ...item, quantity: item.quantity + 1 }
            : item
        );
      } else {
        newItems = [...prevItems, { product, quantity: 1 }];
      }
      
      saveToLocalStorage(newItems);
      return newItems;
    });
  }
};
```

**Почему сначала обновляем state, потом сохраняем?**

Это **оптимистичный UI**. Пользователь жмёт "добавить" и сразу видит +1 товар. Не ждёт ответа от сервера (который может идти 200ms). Это создаёт ощущение мгновенного отклика.

**А если запрос упал?**

Тогда можно откатить изменения (показать ошибку и убрать товар). Но в 99% случаев запрос успешен, и пользователь счастлив от быстроты интерфейса.

#### updateQuantity и removeFromCart — аналогично

Логика такая же: "Если юзер → на сервер, если гость → в localStorage".

### Почему это называется "Facade" (Фасад)?

Паттерн проектирования **Facade** скрывает сложность за простым интерфейсом.

Для компонентов API простой:

```typescript
const { addToCart, items } = useCart();
// Просто вызываем addToCart(product)
```

Но внутри хука — сложная логика:

- Проверка авторизации
- Выбор хранилища
- Обработка ошибок
- Синхронизация state и storage

Компоненты **не знают** об этой сложности. Для них хук — это "чёрный ящик", который просто работает. Это хорошо, потому что:

- Легче тестировать компоненты (не надо мокать 100 вещей)
- Легче менять реализацию (если завтра решим использовать IndexedDB вместо localStorage — компоненты не изменятся)

### Итог коммита №2

✅ Корзина гостя сохраняется в localStorage  
✅ Корзина юзера сохраняется на сервере  
✅ Компоненты не знают о разнице  
✅ При смене пользователя корзина автоматически перезагружается  

Но есть проблема: при логине гость теряет свою корзину! Исправим в следующем коммите.

---

## Коммит 3: Синхронизация при входе

### Проблема

Вася-гость добавил 5 товаров. Они в localStorage. Он решает зарегистрироваться. После входа вызывается `fetchCart()`, который **загружает корзину с сервера** (она пустая). 5 товаров из localStorage **игнорируются**. Вася в ярости.

### Решение: Merge при логине

Нам нужно **перед** вызовом `fetchCart()` отправить локальные товары на сервер методом `merge`.

### Где это делать?

Логично — в `LoginPage`, при успешном логине. Но есть **тонкость с порядком действий**:

```typescript
const handleSubmit = async (e) => {
  e.preventDefault();
  
  // 1. Получаем токен
  const response = await authApi.login(formData);
  
  // 2. ПЕРЕД сохранением токена — синхронизируем!
  await syncLocalCartWithServer(response.access_token);
  
  // 3. Сохраняем токен и загружаем профиль
  await login(response.access_token);
  
  // 4. Обновляем корзину
  await refreshCart();
  
  // 5. Редирект
  navigate(from);
};
```

**Почему именно в таком порядке?**

Если бы мы сделали `login()` перед `sync`, то контекст `AuthContext` обновился бы (`user` стал не `null`), и `useCart` уже считал бы пользователя авторизованным. `refreshCart()` загрузил бы пустую корзину с сервера, **до** того как мы успели сделать merge.

Поэтому merge делаем **до** обновления контекста.

### Функция syncLocalCartWithServer

```typescript
const syncLocalCartWithServer = async (token: string): Promise<void> => {
  try {
    // 1. Читаем localStorage
    const storedCart = localStorage.getItem(CART_STORAGE_KEY);
    if (!storedCart) return; // Пусто → нечего синхронизировать
    
    const localItems = JSON.parse(storedCart);
    if (localItems.length === 0) return;
    
    // 2. Преобразуем в формат API
    const itemsToMerge = localItems.map(item => ({
      product_id: item.product.id,
      quantity: item.quantity
    }));
    
    // 3. Временно устанавливаем токен (для authorizedFetch)
    localStorage.setItem('token', token);
    
    // 4. Отправляем merge
    await cartApi.merge({ items: itemsToMerge });
    
    // 5. Очищаем localStorage
    clearLocalStorageCart();
  } catch (error) {
    console.error('Ошибка синхронизации:', error);
    // НЕ прерываем логин! Даже если merge упал, пользователь должен войти.
  }
};
```

**Почему очищаем localStorage после merge?**

Представь: пользователь залогинился, мы сделали merge, но **не** очистили localStorage. Потом он вышел. Снова зашёл. Опять вызвался merge с **теми же товарами**. На сервере количество удвоилось!

```
Логин #1: localStorage: [A:2, B:1] → merge → сервер: [A:2, B:1]
Выход
Логин #2: localStorage: [A:2, B:1] (не очистили!) → merge → сервер: [A:4, B:2] ❌
```

Очистка решает проблему:

```
Логин #1: localStorage: [A:2, B:1] → merge → сервер: [A:2, B:1] → localStorage очищен
Выход
Логин #2: localStorage: [] → merge пропускается → сервер: [A:2, B:1] ✅
```

### UX: Индикатор синхронизации

Merge может занять секунду (особенно если товаров много). Пользователю нужно объяснить, почему логин "тормозит":

```tsx
{statusMessage && (
  <div className="login-status">
    ⏳ Синхронизируем вашу корзину...
  </div>
)}
```

Это **честность перед пользователем**. Мы не скрываем процессы, а показываем, что происходит. Это создаёт доверие.

### Итог коммита №3

✅ Гость не теряет товары при логине  
✅ Merge интеллектуально суммирует количество  
✅ Пользователь видит процесс синхронизации  
✅ localStorage очищается, чтобы не дублировать  

---

## Коммит 4: Оформление заказа

### Проблема

Корзина есть, товары есть, но **купить нельзя**. Нет формы оформления заказа.

### Что нужно от пользователя?

Минимум информации:

- **Адрес доставки** (обязательно) — куда везти
- **Телефон** (опционально) — для связи с курьером

Список товаров **НЕ** нужен — сервер сам возьмёт из корзины.

### Модальное окно vs отдельная страница

**Вариант 1: Отдельная страница** (`/checkout`)  
✅ Можно сделать многошаговую форму (адрес → оплата → подтверждение)  
✅ Можно дать ссылку (поделиться)  
❌ Дополнительный клик (переход)  
❌ Сложнее навигация (назад из чекаута)  

**Вариант 2: Модальное окно**  
✅ Быстро (нет перехода)  
✅ Контекст корзины на фоне (пользователь видит, что заказывает)  
❌ Нельзя дать ссылку  

Мы выбрали модальное окно, потому что форма простая (2 поля) и это быстрее.

### Реализация модального окна

```tsx
{showCheckoutForm && (
  <div className="checkout-modal-overlay" onClick={() => setShowCheckoutForm(false)}>
    <div className="checkout-modal" onClick={(e) => e.stopPropagation()}>
      {/* Форма */}
    </div>
  </div>
)}
```

**Почему `e.stopPropagation()`?**

Без этого клик на форме "всплыл" бы до оверлея и закрыл окно:

```
Пользователь кликает на input внутри формы
    ↓
Событие click на input
    ↓ (всплытие)
Событие click на .checkout-modal
    ↓ (всплытие)
Событие click на .checkout-modal-overlay → setShowCheckoutForm(false)
    ↓
Модалка закрылась ❌
```

`stopPropagation()` останавливает всплытие — клик не доходит до оверлея:

```
Пользователь кликает на input
    ↓
Событие click на input
    ↓ (всплытие)
Событие click на .checkout-modal → e.stopPropagation() ✋
    ❌ Дальше не идёт
    
Модалка осталась открыта ✅
```

### Процесс оформления

```typescript
const handleCheckout = async (e) => {
  e.preventDefault();
  
  try {
    // 1. Отправляем заказ
    const order = await ordersApi.create({
      delivery_address: deliveryAddress,
      phone: phone || undefined
    });
    
    // 2. Показываем успех
    setCreatedOrder(order);
    
    // 3. Очищаем локальную корзину
    clearCart();
    
    // Бэкенд уже очистил корзину на сервере!
  } catch (error) {
    setOrderError(error.message);
  }
};
```

**Почему не передаём список товаров?**

Безопасность. Представь, что злоумышленник отредактировал JS в браузере и отправил:

```json
{
  "delivery_address": "...",
  "items": [
    { "product_id": 1, "quantity": 10, "price": 0.01 }  // Поменял цену!
  ]
}
```

Если бы сервер доверял этим данным, получилась бы уязвимость. Поэтому сервер **игнорирует** то, что присылает клиент, и **сам** берёт корзину из БД, привязанную к user_id из токена. Подделать токен нельзя (он подписан), значит уязвимости нет.

### Экран успеха

После создания заказа показываем:

```tsx
if (createdOrder) {
  return (
    <div className="order-success">
      <h1>🎉 Заказ успешно оформлен!</h1>
      <p>Номер заказа: #{createdOrder.id}</p>
      <p>Сумма: {createdOrder.total_amount} шмеклей</p>
      <Link to="/profile">Посмотреть мои заказы</Link>
      <Link to="/products">Продолжить покупки</Link>
    </div>
  );
}
```

**Почему не редирект сразу?**

Потому что пользователь хочет **подтверждения**. Если сразу редирект — у него возникнет сомнение: "А заказ точно создался?". Экран успеха даёт уверенность.

### Итог коммита №4

✅ Можно оформить заказ  
✅ Корзина очищается после оформления  
✅ Показываем номер заказа  
✅ Безопасная логика (сервер не доверяет клиенту)  

---

## Коммит 5: История заказов

### Проблема

Пользователь оформил заказ, получил номер #42, но дальше — пустота. Где посмотреть статус? Что заказал? Когда?

### Решение: Раздел "Мои заказы" в профиле

В `ProfilePage` добавляем:

1. Загрузку заказов (`ordersApi.getAll()`)
2. Список с аккордеоном (клик → развернуть детали)
3. Форматирование дат и статусов

### Загрузка заказов

```typescript
const [orders, setOrders] = useState<Order[]>([]);
const [isLoadingOrders, setIsLoadingOrders] = useState(true);

useEffect(() => {
  const fetchOrders = async () => {
    try {
      const userOrders = await ordersApi.getAll();
      // Сортируем по дате (новые первые)
      userOrders.sort((a, b) => 
        new Date(b.created_at).getTime() - new Date(a.created_at).getTime()
      );
      setOrders(userOrders);
    } catch (error) {
      console.error(error);
      setOrdersError(error.message);
    } finally {
      setIsLoadingOrders(false);
    }
  };

  if (user) {
    fetchOrders();
  }
}, [user]);
```

**Почему сортируем на фронте?**

Можно было бы попросить бэкенд отдавать отсортированные данные (`/orders/?sort=created_at:desc`). Но:

1. Наш бэкенд не поддерживает такой параметр (пока)
2. Заказов обычно немного (десятки, не тысячи) — сортировка не тяжёлая
3. Проще сделать на фронте, чем менять бэкенд

Если бы заказов были тысячи — да, сортировка на бэкенде обязательна (с пагинацией).

### Форматирование даты

С сервера приходит ISO строка: `"2025-11-26T15:30:00"`. Пользователю это не читать. Нужно: `"26 ноября 2025, 15:30"`.

```typescript
const formatDate = (isoDate: string): string => {
  const date = new Date(isoDate);
  return date.toLocaleDateString('ru-RU', {
    day: 'numeric',
    month: 'long',      // "ноября" вместо "11"
    year: 'numeric',
    hour: '2-digit',
    minute: '2-digit'
  });
};
```

**Почему `'ru-RU'`?**

Это **локаль** (язык и регион). Если бы указали `'en-US'`, получили бы `"November 26, 2025, 3:30 PM"`. Мы русский магазин — нужен русский формат.

### Статусы заказа

```typescript
const getStatusLabel = (status: Order['status']) => {
  const statuses = {
    pending: { label: 'Ожидает обработки', emoji: '⏳' },
    confirmed: { label: 'Подтверждён', emoji: '✅' },
    shipped: { label: 'Отправлен', emoji: '📦' },
    delivered: { label: 'Доставлен', emoji: '🎉' },
    cancelled: { label: 'Отменён', emoji: '❌' }
  };
  return statuses[status];
};
```

**Зачем эта функция?**

Чтобы не писать `if-else` в JSX. Это **декларативный подход** — описываем соответствия в одном месте, а используем везде.

### Паттерн Аккордеон

```typescript
const [expandedOrderId, setExpandedOrderId] = useState<number | null>(null);

const toggleOrderDetails = (orderId: number) => {
  setExpandedOrderId(prev => 
    prev === orderId 
      ? null      // Был открыт → закрываем
      : orderId   // Был закрыт или открыт другой → открываем этот
  );
};
```

**Почему только один открытый?**

Это **эксклюзивный аккордеон** — открыт максимум один элемент. Альтернатива — **множественный**, где можно открыть несколько.

Мы выбрали эксклюзивный, потому что:
✅ Экономит место (страница не "растягивается")  
✅ Фокусирует внимание на одном заказе  

Для истории покупок это удобнее — обычно смотрят последний заказ, а не все сразу.

### Отображение товаров в заказе

```tsx
<ul className="order-items-list">
  {order.items.map(item => (
    <li key={item.id} className="order-item">
      <span>{item.product_name}</span>
      <span>
        {item.quantity} × {item.frozen_price} = 
        <strong>{(item.quantity * item.frozen_price).toFixed(2)}</strong> шм.
      </span>
    </li>
  ))}
</ul>
```

**Почему используем `frozen_price`, а не `product.price`?**

Потому что в заказе товар может стоить 500 шмеклей (это `frozen_price` на момент заказа), а в таблице `products` цена уже 600. Если бы показали текущую цену — пользователь запутался бы: "Я платил 500 или 600?".

`frozen_price` — это гарантия: "Вот сколько ты заплатил в тот момент".

### Итог коммита №5

✅ Пользователь видит все свои заказы  
✅ Может развернуть детали любого заказа  
✅ Видит зафиксированные цены  
✅ Даты читабельные (не ISO строки)  

---

## Выводы и принципы проектирования

### Что мы сделали?

Превратили простую витрину в полноценный e-commerce клиент:

| Было | Стало |
|------|-------|
| Корзина в памяти (пропадает при перезагрузке) | Гибридное хранилище (localStorage + сервер) |
| Нет заказов | Полноценное оформление с адресом |
| Нет истории | Раздел "Мои заказы" с деталями |
| Одно устройство | Синхронизация между устройствами |
| Гость теряет товары при логине | Merge сохраняет всё |

### Ключевые принципы, которые мы применили

#### 1. Единый источник правды (Single Source of Truth)

Для авторизованных пользователей правда всегда на сервере. Локальный state — это кеш для быстрого отображения.

**Почему это важно?** Если бы правда была в двух местах (сервер + localStorage), они бы рассинхронизировались, и мы не знали бы, какой версии верить.

#### 2. Паттерн Facade (Фасад)

Хук `useCart` скрывает сложность (выбор хранилища, авторизация, ошибки) за простым интерфейсом (`addToCart`, `items`).

**Почему это важно?** Компоненты не должны знать о внутренностях. Если завтра мы заменим localStorage на IndexedDB — компоненты не изменятся.

#### 3. Оптимистичный UI

Сначала обновляем интерфейс, потом отправляем запрос. Если запрос упал — откатываем.

**Почему это важно?** Пользователь видит мгновенную реакцию, даже если сеть медленная. Это создаёт ощущение быстроты.

#### 4. Graceful Degradation (Постепенная деградация)

Если сервер недоступен (нет интернета), гость всё равно может добавлять товары в localStorage. Сайт не "ломается", а работает в ограниченном режиме.

**Почему это важно?** Лучше частичная функциональность, чем белый экран с ошибкой.

#### 5. Frozen Price (Замороженная цена)

При создании заказа фиксируем текущую цену товара. Даже если завтра цена изменится, в заказе останется старая.

**Почему это важно?** Юридическая защита. Клиент купил по 500 — в договоре (заказе) должно быть 500, а не текущая цена.

#### 6. Upsert (Update + Insert)

При merge не просто добавляем товары, а интеллектуально объединяем: если товар есть → увеличиваем количество, если нет → добавляем новый.

**Почему это важно?** Пользователь не теряет данные. Его 2 товара + сервер 1 товар = 3 товара (а не замена 2→1).

#### 7. DRY (Don't Repeat Yourself)

Общая логика авторизованных запросов вынесена в `authorizedFetch`. Не дублируем код в каждом методе.

**Почему это важно?** Если нужно добавить логирование или обработку ошибок — меняем в одном месте, а не в десяти.

### Что дальше?

Это рабочая система, но можно улучшить:

1. **Пагинация заказов** — если их сотни, не грузим все сразу
2. **Фильтрация по статусу** — показать только доставленные/ожидающие
3. **Отмена заказа** — если статус `pending`, дать кнопку отмены
4. **Уведомления** — при изменении статуса заказа (WebSocket или long polling)
5. **Кеширование** — не загружать заказы при каждом заходе в профиль

Но это уже следующие итерации. Главное — **фундамент крепкий**, и на него легко наращивать функционал.

---

*Конспект создан: 26 ноября 2025*  
*Автор: VladimirMonin для курса React Router 419*
